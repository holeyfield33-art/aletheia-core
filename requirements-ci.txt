# Lightweight deps for CI — excludes torch and sentence-transformers
# These are excluded because test_api.py (which needs them) is ignored in CI
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5
python-dotenv==1.2.2
cryptography==46.0.7
pyyaml==6.0.3
numpy==2.4.3
pytest==9.0.3
httpx==0.28.1
pytest-asyncio==1.3.0
hypothesis==6.152.3
confusable-homoglyphs==3.3.1
